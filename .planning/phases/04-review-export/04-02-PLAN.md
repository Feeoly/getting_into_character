---
phase: 04-review-export
plan: 2
type: execute
wave: 2
depends_on:
  - 3
files_modified:
  - src/app/session/[id]/review/[takeId]/page.tsx
  - src/app/session/[id]/transcript/[takeId]/page.tsx
  - src/app/session/[id]/rehearsal/_lib/transcription/TranscriptSummaryCard.tsx
  - src/app/session/[id]/rehearsal/_lib/transcription/sttCopy.ts
autonomous: true
requirements:
  - REVI-01
user_setup: []
must_haves:
  truths:
    - 存在 `/session/[id]/review/[takeId]` 客户端页：转写列表 + 按 take 的停顿列表 + 可选媒体播放器与时间点跳转
    - 全文转写页与摘要卡片可进入复盘（链接文案与 04-UI-SPEC / reviewCopy 一致）
    - 片段与停顿行为与 03 一致：文本仅文本节点，防 XSS
  artifacts:
    - path: src/app/session/[id]/review/[takeId]/page.tsx
      provides: 复盘主界面壳（Chat/导出/删除可留占位或 TODO 接 04-03）
    - path: src/app/session/[id]/rehearsal/_lib/transcription/sttCopy.ts
      provides: 新增 `review.*` 文案键（或独立 `reviewCopy.ts`）
  key_links:
    - from: review page
      to: listSegmentsForTake / listPauseEventsForTake / getLatestJobForTake
      via: sessionId + takeId 路由参数校验（uuid）
---

<objective>
实现 **04-CONTEXT D-01** 复盘主路由 **`/session/[id]/review/[takeId]`**：**转写片段列表**（复用全文页 `fmt`/`listSegmentsForTake` 模式）、**停顿列表**（`listPauseEventsForTake`）、**可选 `<audio>`/`<video>`**（自 `getLatestJobForTake` 的 `audioBlob` + mime，Object URL 生命周期正确）。点击片段或停顿行 → **`currentTime`** 跳转（REVI-01）。**全文页**与 **TranscriptSummaryCard** 增加 **「进入复盘」** 链到该路由。文案键集中（`sttCopy` 扩展或新建 `reviewCopy.ts`），对齐 **04-UI-SPEC**。
</objective>

<context>
- **布局：** 桌面可用主栏（转写+媒体）+ 侧栏（停顿）；窄屏上下堆叠（Claude discretion）。Chat/导出/删除 UI 若本 wave 仅搭壳，用占位区并在 **04-03** 填满。
- **路由校验：** `takeId`/`sessionId` 与 transcript 页一致（uuid）；非法 `notFound()` 或内联错误态择一。
- **无 Blob：** 仅展示转写+停顿，无播放器；列表仍可只读。
- **高亮：** `timeupdate` 高亮当前片段为可选增强。
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 转写文本 → DOM | 同 Phase 3，禁止 `dangerouslySetInnerHTML` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | T | 片段渲染 | mitigate | 文本节点 / React 默认转义 |
| T-04-02-02 | I | Object URL | mitigate | `useEffect` cleanup `revokeObjectURL` |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: reviewCopy（或 sttCopy 扩展）+ 复盘页数据加载</name>
  <read_first>
    - .planning/phases/04-review-export/04-UI-SPEC.md
    - src/app/session/[id]/transcript/[takeId]/page.tsx
    - src/app/session/[id]/rehearsal/_lib/transcription/transcriptRepo.ts
    - src/app/session/[id]/rehearsal/_lib/rehearsalRepo.ts
  </read_first>
  <acceptance_criteria>
    - 新建 `src/app/session/[id]/review/[takeId]/page.tsx`（`"use client"`）
    - `rg "listSegmentsForTake|listPauseEventsForTake" src/app/session/[id]/review` 至少 2 处匹配
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
  <files>
    src/app/session/[id]/review/[takeId]/page.tsx
    src/app/session/[id]/rehearsal/_lib/transcription/sttCopy.ts
  </files>
  <action>
    增加 zh-CN 常量：`review.sectionTitle`、`review.openReview`、`review.noPauses` 等（对照 UI-SPEC 文案表）。复盘页：`getSessionById`；`listSegmentsForTake`；`listPauseEventsForTake`；`getLatestJobForTake` 判断是否可播。媒体：`useRef` + `useEffect` 创建/销毁 blob URL。
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    复盘页可展示本轮转写与停顿。
  </done>
</task>

<task type="auto">
  <name>Task 2: 交互 — 跳转 currentTime + 导航入口</name>
  <read_first>
    - .planning/phases/04-review-export/04-UI-SPEC.md
    - src/app/session/[id]/transcript/[takeId]/page.tsx
    - src/app/session/[id]/rehearsal/_lib/transcription/TranscriptSummaryCard.tsx
  </read_first>
  <acceptance_criteria>
    - transcript 页存在指向 `/session/[id]/review/[takeId]` 的 `Link`
    - `TranscriptSummaryCard` 或会话详情区有「进入复盘」主/次链（至少一处）
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
  <files>
    src/app/session/[id]/review/[takeId]/page.tsx
    src/app/session/[id]/transcript/[takeId]/page.tsx
    src/app/session/[id]/rehearsal/_lib/transcription/TranscriptSummaryCard.tsx
  </files>
  <action>
    片段行/停顿行 `onClick`：`mediaRef.current.currentTime = start_ms/1000`（有播放器时）。全文页顶部或底部工具条加 **进入复盘**。摘要卡片在 `succeeded`（及可选 `failed` 仍有 take时）提供复盘链接。
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>rg "review/\\[takeId\\]|/review/" src/app/session/[id]/transcript src/app/session/[id]/rehearsal/_lib/transcription/TranscriptSummaryCard.tsx</automated>
  </verify>
  <done>
    REVI-01 导航与跳转闭环可用。
  </done>
</task>

</tasks>

## Wave 2 Verification

```bash
npm run typecheck
npm run lint
```

手工：从会话详情 → 全文 → 进入复盘；点击片段观察播放器 seek（若有媒体）。

---

*Plan 04-02 · Phase 04 wave 2*
