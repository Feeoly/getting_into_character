---
phase: 03-local-stt
plan: 3
type: execute
wave: 3
depends_on:
  - 2
files_modified:
  - src/app/session/[id]/page.tsx
  - src/app/session/[id]/transcript/[takeId]/page.tsx
  - src/app/session/[id]/rehearsal/_lib/transcription/sttCopy.ts
  - src/app/session/[id]/rehearsal/_lib/transcription/TranscriptSummaryCard.tsx
  - src/app/session/[id]/rehearsal/_lib/transcription/SttToast.tsx
  - src/app/session/[id]/rehearsal/page.tsx
  - src/app/session/[id]/rehearsal/_lib/transcription/transcriptionRunner.ts
autonomous: true
requirements:
  - STT-01
  - STT-02
  - STT-03
user_setup: []
must_haves:
  truths:
    - 会话详情出现「转写」摘要卡片，状态与 UI-SPEC 文案键一致
    - 存在只读「全文转写」路由，片段按 `idx` 展示 `mm:ss` + 文本；HTML 转义防 XSS
    - 转写失败时非阻塞 Toast +「重试转写」；与 D-06 一致
  artifacts:
    - path: src/app/session/[id]/page.tsx
      provides: 摘要卡片 + 导航入口
    - path: src/app/session/[id]/transcript/[takeId]/page.tsx
      provides: 全文只读页
    - path: src/app/session/[id]/rehearsal/_lib/transcription/sttCopy.ts
      provides: UI-SPEC 字符串常量（zh-CN）
  key_links:
    - from: TranscriptSummaryCard
      to: transcriptRepo.getLatestJobForSession / listSegmentsForTake
      via: useEffect + sessionId
    - from: SttToast
      to: transcriptionRunner 重试 API
      via: onRetry 回调
---

<objective>
按 **03-UI-SPEC.md** 完成 **会话详情转写摘要卡片**、**`/session/[id]/transcript/[takeId]` 只读全文页**、以及 **失败 Toast + 内联重试**（与 `transcriptionRunner` / repo 衔接）。转写正文渲染使用 **React 默认文本转义**（禁止 `dangerouslySetInnerHTML`）。**STT-03**：页内展示 `fullPageHint` 固定句；验收说明含「无默认远端 STT」与 Network 观察。
</objective>

<context>
- 文案：严格使用 `03-UI-SPEC.md` §Phase 3 文案表（可集中 `sttCopy.ts` 导出常量）。
- 视觉：卡片 secondary 表面、`tabular-nums`、indeterminate 进度条、`h-1` 等按 UI-SPEC。
- 「重新转写」：若 02 采用 **覆盖策略**，在全文页或摘要区提供 `stt.retranscribe` + `window.confirm` 使用 UI-SPEC Destructive 确认文案；确认后对新 job 入队（同 `takeId` + 新 `jobId`，由 repo 替换逻辑处理）。
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 片段文本 → DOM | 用户转写可能含 `&lt;script&gt;` 样字符，必须不能执行 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | T | 全文页片段列表 | mitigate | 仅 `{segment.text}` 文本节点或经 `escape` 的工具函数；禁止 HTML 注入 |
| T-03-03-02 | I | `takeId` 路由参数 | mitigate | Zod `uuid` 或 `min(1)` 校验；非法则 `notFound()` |
| T-03-03-03 | E | Toast 高频失败 | mitigate | 重试退避（如 attempt 上限 3）+ 文案提示录音仍保留 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: sttCopy + TranscriptSummaryCard + 嵌入 session/[id]/page.tsx</name>
  <read_first>
    - .planning/phases/03-local-stt/03-UI-SPEC.md
    - src/app/session/[id]/page.tsx
    - src/app/session/[id]/rehearsal/_lib/transcription/transcriptRepo.ts
  </read_first>
  <acceptance_criteria>
    - `rg "stt.sectionTitle|查看全文转写|转写" src/app/session/[id]/page.tsx src/app/session/[id]/rehearsal/_lib/transcription/TranscriptSummaryCard.tsx src/app/session/[id]/rehearsal/_lib/transcription/sttCopy.ts` 至少 2 处匹配
    - `rg "dangerouslySetInnerHTML" src/app/session/[id]/rehearsal/_lib/transcription/TranscriptSummaryCard.tsx` 退出码 1（文件中不得出现）
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
  <files>
    src/app/session/[id]/page.tsx
    src/app/session/[id]/rehearsal/_lib/transcription/sttCopy.ts
    src/app/session/[id]/rehearsal/_lib/transcription/TranscriptSummaryCard.tsx
  </files>
  <action>
    卡片：标题 `stt.sectionTitle`、状态 pill 映射 `queued|processing|succeeded|failed` → 文案表 `stt.job*`；`processing/queued` 显示 `summaryLoading` + **indeterminate** `h-1` 条；`succeeded` 显示 `summaryPreview`（拼接 segments 文本截断 **120–160 字**）；`failed` 显示 Error 文案 + `inlineRetry`。主链 `stt.viewFull` → `Link` 到 `/session/[id]/transcript/[takeId]`（`takeId` 取当前展示轮次：默认 **最近 succeeded job 的 takeId**，若无则最近一条 job 的 `takeId`）。**不得**使用 `dangerouslySetInnerHTML`。
  </action>
  <verify>
    <automated>rg "stt.sectionTitle|查看全文转写|转写" src/app/session/[id]/page.tsx src/app/session/[id]/rehearsal/_lib/transcription/TranscriptSummaryCard.tsx src/app/session/[id]/rehearsal/_lib/transcription/sttCopy.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    会话详情展示转写摘要与进入全文入口；无 HTML 注入路径。
  </done>
</task>

<task type="auto">
  <name>Task 2: 只读全文页 transcript/[takeId]/page.tsx</name>
  <read_first>
    - .planning/phases/03-local-stt/03-UI-SPEC.md
    - src/app/session/[id]/rehearsal/_lib/transcription/transcriptRepo.ts
    - src/app/session/[id]/rehearsal/_lib/transcription/sttCopy.ts
  </read_first>
  <acceptance_criteria>
    - 文件 `src/app/session/[id]/transcript/[takeId]/page.tsx` 存在且 `rg "全文转写|以下为本地生成" src/app/session/[id]/transcript/` 退出码 0
    - `rg "dangerouslySetInnerHTML" src/app/session/[id]/transcript/` 退出码 1
    - `npm run lint` 退出码 0
  </acceptance_criteria>
  <files>
    src/app/session/[id]/transcript/[takeId]/page.tsx
  </files>
  <action>
    Client Component：`takeId` 校验失败展示会话级错误。加载 `listSegmentsForTake(sessionId, takeId)` 升序 `idx`。时间列固定宽 `72px`–`88px`，`tabular-nums`。空态 `stt.emptyFull`。页脚或顶栏：`stt.retranscribe` 调用 `retryTranscription(sessionId, takeId)`（在 runner 或 repo 暴露：从 job 行读 `audioBlob` 重新 `enqueue`）。**XSS**：仅用 React 文本绑定。背景分割线若需要：用 **高 4px** `#E2E8F0` 条（遵守 UI-SPEC「禁止 hairline 文案」指契约不写 1px，实现用 4px）。
  </action>
  <verify>
    <automated>test -f src/app/session/[id]/transcript/[takeId]/page.tsx</automated>
    <automated>rg "全文转写|以下为本地生成" src/app/session/[id]/transcript/</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>
    全文只读列表与 STT-02 时间展示就绪；路由参数校验。
  </done>
</task>

<task type="auto">
  <name>Task 3: SttToast + initModel 细条 + runner 失败订阅</name>
  <read_first>
    - src/app/session/[id]/rehearsal/_ui/PauseToast.tsx
    - src/app/session/[id]/rehearsal/page.tsx
    - src/app/session/[id]/rehearsal/_lib/transcription/transcriptionRunner.ts
    - .planning/phases/03-local-stt/03-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - `rg "toastFailTitle|转写失败|重试转写" src/app/session/[id]/rehearsal/` 退出码 0
    - `rg "initModel|正在准备离线识别" src/app/session/[id]/rehearsal/` 退出码 0
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
  <files>
    src/app/session/[id]/rehearsal/_lib/transcription/SttToast.tsx
    src/app/session/[id]/rehearsal/page.tsx
    src/app/session/[id]/rehearsal/_lib/transcription/transcriptionRunner.ts
  </files>
  <action>
    复用 `PauseToast` 的定位/样式模式或抽取共享容器；右下角或底部择一与 `PauseToast` **一致**。`transcriptionRunner` 导出 `onJobFailed` 订阅或简单 `mitt` 事件（若不想加依赖，用 `window` 自定义事件 **仅 dev 慎用**——优先 **回调注册表** 单例）。排练页顶部/底部 `min-h-[40px]` 展示 `initModel`/`initModelSub` 当 `processing` 且模型未就绪标志位（由 runner 设置）。Toast 主按钮触发 `retryTranscription`（同 `takeId` 新 job）。**验收注释**：在 `sttCopy.ts` 或 runner 顶部文件注释列出 STT-03 Network 检查步骤（中文 2 条即可）。
  </action>
  <verify>
    <automated>rg "toastFailTitle|转写失败|重试转写" src/app/session/[id]/rehearsal/</automated>
    <automated>rg "initModel|正在准备离线识别" src/app/session/[id]/rehearsal/</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    失败非阻塞提示与重试路径可用；首次模型加载有低焦虑说明。
  </done>
</task>

</tasks>

<verification>
- 手动：会话详情 → 查看全文 → 刷新页面仍恢复（同 URL）。
- 手动：DevTools → Network → 过滤 `fetch`/`xhr`，确认**无**非 HF 静态权重之外的 STT 云 API。
- `npm run lint && npm run typecheck` 通过。
</verification>

<success_criteria>
- UI 字符串与 `03-UI-SPEC.md` 表中 key 语义一致（允许标点全角半角一致化但语义不变）。
- 失败路径用户可见 **Toast + 重试**，无静默失败。
</success_criteria>
