# Phase 1: 信任基线与会话骨架 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.  
> Decisions are captured in `01-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-04-10  
**Phase:** 01-信任基线与会话骨架  
**Areas discussed:** 信息架构（首页/历史/详情）, 本地优先/隐私信任 UX, 本地数据保存策略

---

## 信息架构（首页/历史/详情）

| Option | Description | Selected |
|--------|-------------|----------|
| A. 以“新建排练”为主 + 下方历史列表 | 默认路径最短，开门见山 |  |
| B. 以“继续上次”为主（若存在）+ 新建按钮 | 强调连续性 |  |
| C. 先是介绍页/引导页，再进入新建 | 先解释“角色扮演/隐私本地”，再进入会话 | ✓ |

**历史列表字段（多选）：**
- 选中：场景、创建时间、状态、时长（Phase 1 可占位）、备注/名称（需要尽量不打断）

---

## 本地优先/隐私信任 UX

| Option | Description | Selected |
|--------|-------------|----------|
| A. 顶部常驻一行提示（可收起） | 强信任信号但占空间 |  |
| B. 仅在关键按钮附近显示（如“开始排练”旁） | 更克制，避免干扰 | ✓ |
| C. 首次进入弹窗确认一次 | 明确但打断流程 |  |

---

## 本地数据保存策略

| Option | Description | Selected |
|--------|-------------|----------|
| A. 默认永久保留（用户手动删除） | 最简单、符合练习记录心智 | ✓ |
| B. 只保留最近 N 次（可配置） | 省空间但可能误删 |  |
| C. 只保留最近 X 天（可配置） | 临时练习心智，不利长期进步 |  |

**一键清空所有历史：** 本阶段不做。

---

## Claude's Discretion

- 引导页文案与布局密度
- 历史列表默认排序
- “备注/名称”录入方式（创建时 vs 创建后）

---

## 增补会话：全站导航层级与用语

**Date:** 2026-04-11  
**Phase:** 01-信任基线与会话骨架  
**Areas discussed:** 路由层级、返回首页/返回会话文案与位置、主按钮与次要链的视觉权重、错误态

**触发：** 用户提出「返回首页」仅在长页底部、需滚动才可见；要求通过 `/gsd-discuss-phase` 锁定导航层级与用语。

---

## 页面层级与「应用根」

| Option | Description | Selected |
|--------|-------------|----------|
| A. 首页仅等于历史列表区域，与 `/` 分离 | 易产生两个「根」 |  |
| B. **`/` 统一为首页**（引导 + 历史）；子页返回根一律指向 `/` | 单一出口、与现有路由一致 | ✓ |

---

## 「返回首页」位置与唯一性

| Option | Description | Selected |
|--------|-------------|----------|
| A. 仅保留页面底部主按钮 | 长页难发现 |  |
| B. **首屏顶部信息区至少一处「返回首页」**；底部不得作为**唯一**出口 | 符合工具型 Web 离开路径 | ✓ |
| C. 吸底固定栏 | 非必要复杂度 |  |

**User's choice:** B（并允许底部重复弱化或移除大按钮，以顶部为准）  
**Notes:** 避免用户滚过危险区后才看到离开入口。

---

## 回到会话枢纽的文案

| Option | Description | Selected |
|--------|-------------|----------|
| A. 各页自拟（返回详情 / 回上一页等） | 不一致 |  |
| B. **统一「返回会话」→ `/session/{id}`** | 与 `review.backToSession` 等现有文案对齐 | ✓ |

---

## 导航控件的视觉权重

| Option | Description | Selected |
|--------|-------------|----------|
| A. 「返回首页」与主 CTA 同级实心按钮 | 抢首要任务 |  |
| B. **返回类默认次要文字链或 outline**；实心按钮留给当前页首要任务 | 层次清晰 | ✓ |

---

## Claude's Discretion（本次增补）

- 顶部导航具体排版（标题上一行 vs 同行）、未来是否引入全局顶栏/面包屑
