---
phase: 01-trust-baseline
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/app/_ui/BackToHomeLink.tsx
  - src/app/session/new/page.tsx
  - src/app/session/[id]/page.tsx
  - src/app/session/[id]/rehearsal/page.tsx
  - src/app/session/[id]/review/[takeId]/page.tsx
  - src/app/session/[id]/transcript/[takeId]/page.tsx
  - src/app/session/[id]/role/read/page.tsx
autonomous: true
requirements: [ENTR-03, PRIV-02]
must_haves:
  truths:
    - "所有常规内容页首屏可见区有文案「返回首页」链向 /，且长页底部不再作为唯一出口（D-08）"
    - "子路由回到 `/session/{id}` 的文案保持「返回会话」，与 sttCopy / roleCopy 一致（D-09）"
    - "返回类链为次要视觉；当前页首要任务保留实心主按钮（D-10）"
    - "PRIV-02（内联隐私说明）仍由 01-01/01-02 交付；本计划不移动或删减隐私文案，仅改善导航可达性以支撑「状态/信任可见」"
    - "D-11：复盘/转写/角色朗读等短错误态已有「返回首页」链，本计划不强制改动；排练页 notFound 须补齐回根链（见 Task 4）"
  artifacts:
    - path: "src/app/_ui/BackToHomeLink.tsx"
      provides: "可复用的「返回首页」链（含浅色页与排练页深色顶栏变体）"
  key_links:
    - from: "src/app/session/[id]/page.tsx"
      to: "src/app/_ui/BackToHomeLink.tsx"
      via: "import"
      pattern: "BackToHomeLink"
    - from: "src/app/session/new/page.tsx"
      to: "src/app/_ui/BackToHomeLink.tsx"
      via: "import"
      pattern: "BackToHomeLink"
    - from: "src/app/session/[id]/review/[takeId]/page.tsx"
      to: "src/app/_ui/BackToHomeLink.tsx"
      via: "import"
      pattern: "BackToHomeLink"
    - from: "src/app/session/[id]/transcript/[takeId]/page.tsx"
      to: "src/app/_ui/BackToHomeLink.tsx"
      via: "import"
      pattern: "BackToHomeLink"
    - from: "src/app/session/[id]/rehearsal/page.tsx"
      to: "src/app/_ui/BackToHomeLink.tsx"
      via: "import"
      pattern: "BackToHomeLink"
    - from: "src/app/session/[id]/role/read/page.tsx"
      to: "src/app/_ui/BackToHomeLink.tsx"
      via: "import"
      pattern: "BackToHomeLink"
---

<objective>
将 `01-CONTEXT.md` 中 **D-07～D-11**（全站导航层级、返回首页位置、返回会话用语、返回控件视觉权重、错误态）落实到现有 Next.js 页面。不改动业务逻辑与数据流，仅调整导航结构与样式层级。**PRIV-02** 不在此计划重写隐私 UI（由 01-01已交付），本计划仅保证主导航不削弱用户回到首页、继续会话路径的可达性。**D-11** 以「排练 notFound 补齐」为增量；其它短错误页若已有 `返回首页` 则保持。

Purpose: 用户无需滚到长页底部即可回到首页；会话子页与复盘/转写/排练/朗读之间层级清晰、用语统一。

Output: 轻量 `BackToHomeLink` 组件 + 各目标页顶栏与底部重复入口的整理。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-trust-baseline/01-CONTEXT.md
@.planning/phases/01-trust-baseline/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 BackToHomeLink 组件</name>
  <read_first>
    - @src/app/_ui/PrimaryButton.tsx（现有按钮/链样式参考）
    - @.planning/phases/01-trust-baseline/01-CONTEXT.md（D-08、D-10文案与权重）
  </read_first>
  <acceptance_criteria>
    - 存在文件 `src/app/_ui/BackToHomeLink.tsx`，默认导出 React 组件
    - 组件渲染指向 `/` 的 Next `Link`，可见子文案为精确字符串 `返回首页`（grep 可验）
    - 支持至少两种 class变体：`variant="default"`（浅底页：`text-sm font-semibold text-blue-600 hover:underline`）与 `variant="onDark"`（排练顶栏：白/浅色字 + hover，不与「设置」实心按钮冲突）
  </acceptance_criteria>
  <files>
    src/app/_ui/BackToHomeLink.tsx
  </files>
  <action>
    - `variant="default" | "onDark"`，其余布局由调用方用外层 `flex`/`gap` 组合。
    - 不使用 `PrimaryButton` 实现「返回首页」。
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    全站可导入统一「返回首页」链，避免每页手写不一致 class。
  </done>
</task>

<task type="auto">
  <name>Task 2: 会话详情与新建会话页顶栏 + 底部去重（D-08、D-10）</name>
  <read_first>
    - @src/app/session/[id]/page.tsx
    - @src/app/session/new/page.tsx
    - @src/app/_ui/BackToHomeLink.tsx
  </read_first>
  <acceptance_criteria>
    - `session/[id]/page.tsx` 在 `<h1>` 所在首屏区域**上方或同行**出现 `BackToHomeLink`（default）
    - `session/new/page.tsx` 同样在首屏标题区可见 `BackToHomeLink`
    - 两页中**移除**底部 `PrimaryButton`且 `href="/"` 的块；执行后 `grep -F 'PrimaryButton' src/app/session/new/page.tsx` 与 `grep -F 'PrimaryButton' 'src/app/session/[id]/page.tsx'` 均无匹配（或仅剩非首页用途，不得再出现 `href=\"/\"`）
    - 两页仍可通过顶栏「返回首页」到达 `/`
  </acceptance_criteria>
  <files>
    src/app/session/[id]/page.tsx
    src/app/session/new/page.tsx
  </files>
  <action>
    - 若移除 `PrimaryButton` 后文件无他用，删除对应 import。
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
    长会话详情页离开首页的入口在首屏可见。
  </done>
</task>

<task type="auto">
  <name>Task 3: 复盘页与全文转写页顶栏 — 返回首页 + 次要化「返回会话」（D-08～D-10、D-09）</name>
  <read_first>
    - @src/app/session/[id]/review/[takeId]/page.tsx
    - @src/app/session/[id]/transcript/[takeId]/page.tsx
    - @src/app/session/[id]/rehearsal/_lib/transcription/sttCopy.ts（`review.backToSession`）
    - @src/app/_ui/BackToHomeLink.tsx
  </read_first>
  <acceptance_criteria>
    - **复盘：**正常内容态 `header` 内（与导出按钮同一 `flex` 区域）存在 `BackToHomeLink`（default），文案 `返回首页`
    - **复盘：** 「返回会话」`Link` 子节点为 `review.backToSession`，样式为 outline（`border border-slate-300 bg-white`），**不得**使用 `bg-blue-600`
    - **转写：** 标题行同区域增加 `BackToHomeLink`（default）；指向 `/session/${id}` 的链为 outline，与「重新转写」「进入复盘」同级，**不得** `bg-blue-600`；文案精确 `返回会话`
  </acceptance_criteria>
  <files>
    src/app/session/[id]/review/[takeId]/page.tsx
    src/app/session/[id]/transcript/[takeId]/page.tsx
  </files>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    复盘与转写长页首屏可回首页；回会话为次要按钮。
  </done>
</task>

<task type="auto">
  <name>Task 4: 排练页顶栏 — 返回首页 + notFound 出口（D-08、D-11）</name>
  <read_first>
    - @src/app/session/[id]/rehearsal/page.tsx
    - @src/app/_ui/BackToHomeLink.tsx
  </read_first>
  <acceptance_criteria>
    - 顶栏 `flex` 区域在「返回会话」**同一行**增加 `BackToHomeLink variant="onDark"`，文案 `返回首页`
    - `notFound` 卡片内除说明文案外，包含指向 `/` 的 `返回首页` 链（可用 `BackToHomeLink` 或 `Link`，须 `text-sm` 级次要样式）
  </acceptance_criteria>
  <files>
    src/app/session/[id]/rehearsal/page.tsx
  </files>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    沉浸排练页首屏可离开到首页；会话不存在时有明确回根路径。
  </done>
</task>

<task type="auto">
  <name>Task 5: 朗读页顶栏 — 返回首页（D-08、D-09）</name>
  <read_first>
    - @src/app/session/[id]/role/read/page.tsx
    - @src/app/session/[id]/_lib/roleCopy.ts
    - @src/app/_ui/BackToHomeLink.tsx
  </read_first>
  <acceptance_criteria>
    - 主内容态（含 `readPageTitle` 的 header）在首屏可见 `BackToHomeLink`（default）与现有 `{role.backToSession}` 链并列（同一 `flex` 行或紧邻块）
    - `role.backToSession` 文案仍为「返回会话」，链接仍为 `/session/${id}`
    - 「我已完成朗读」保持 `bg-blue-600` 主按钮不变
  </acceptance_criteria>
  <files>
    src/app/session/[id]/role/read/page.tsx
  </files>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
    朗读流程中用户可随时回首页或回会话详情。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---------|-------------|
| 导航目标 | 仅 `Link` 到已知内部路径 `/` 与 `/session/...`，不引入外链 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-01-03a | Tampering | 错误 URL 构造 | mitigate | 继续使用 Next `Link` 静态 `href`，不拼接不可信输入到域名外 |
</threat_model>

<verification>
-手工：打开 `/session/{真实id}`，不滚动即可见「返回首页」；滚动到底部无唯一 `PrimaryButton` 式返回（新建页/详情页）
- `npm run typecheck` && `npm run lint` && `npm run build` 通过
</verification>

<success_criteria>
- D-07～D-11 在列出的路由上可观察满足（首屏返回首页、返回会话用语与目标一致、返回链为次要视觉）
</success_criteria>

<output>
After completion, create `.planning/phases/01-trust-baseline/01-03-SUMMARY.md`
</output>
