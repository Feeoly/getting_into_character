---
phase: 05-role-card
plan: 2
type: execute
wave: 2
depends_on:
  - 4
files_modified:
  - src/app/session/[id]/page.tsx
  - src/app/session/[id]/_ui/RoleCardSection.tsx
  - src/app/session/[id]/role/read/page.tsx
  - src/app/session/[id]/rehearsal/page.tsx
autonomous: true
requirements:
  - ROLE-01
  - ROLE-03
user_setup: []
must_haves:
  truths:
    - 会话详情：角色表单 + 保存 + 进入朗读；朗读页展示卡片 + 完成按钮 + 可选 TTS
    - 进入排练：有卡未朗读时 confirm 软阻塞（D-06）
    - 全文纯文本渲染，无 HTML 注入
  artifacts:
    - path: src/app/session/[id]/role/read/page.tsx
      provides: ROLE-03 朗读模式路由
    - path: src/app/session/[id]/_ui/RoleCardSection.tsx
      provides: ROLE-01 输入与生成入口
  key_links:
    - from: RoleCardSection
      to: buildRoleCardText + sessionRepo
      via: 保存后 Link至 /role/read
---

<objective>
落实 **05-CONTEXT D-01、D-02、D-05、D-06**：在 **`/session/[id]`** 增加 **角色与触发物** 区块（默认展开，可折叠）；**保存** 调用 wave1 repo 并 **`Link`** 至 **`/session/[id]/role/read`**。朗读页展示 **`roleCardText`**（**`pre` 或 `whitespace-pre-wrap`** 段落），**「我已完成朗读」** 写入 **`roleReadAloudCompletedAt`** 并 **`router.push`** 排练页；可选 **`speechSynthesis`**。**进入排练**：若存在 **`roleCardText`** 且 **无** `roleReadAloudCompletedAt`，**`window.confirm`** 软提示后可仍进入（D-06）。无角色卡时主 CTA 引导先生成（跳转锚点或禁用排练按钮 + 文案，择一）。
</objective>

<context>
- **UI token：** 与现有会话详情白底卡片、`rounded-lg`、`slate` 色系一致。
- **排练入口：** 将原 `PrimaryButton` 直链排练改为 **客户端** `button` 或包装组件：先 `confirm` 再 `router.push`；无卡时 `alert` 或聚焦角色区（Claude discretion）。
- **朗读页：** 顶部 **返回会话** 链；TTS：`speechSynthesis` 不支持时隐藏「听一遍」。
- **D-07：** 不在本 PLAN 强制改 ReviewChat。
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| roleCardText → DOM | 仅文本节点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | T | 卡片 XSS | mitigate | 禁止 `dangerouslySetInnerHTML` |
| T-05-02-02 | I | 未加载 session 误点 | mitigate | 朗读页加载前校验 `roleCardText` 存在，否则提示回详情 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: RoleCardSection + 嵌入 session/[id]/page.tsx</name>
  <read_first>
    - .planning/phases/05-role-card/05-CONTEXT.md
    - src/app/session/[id]/page.tsx
    - src/app/session/[id]/_lib/buildRoleCardText.ts
    - src/app/_lib/sessionRepo.ts
  </read_first>
  <acceptance_criteria>
    - 详情页在排练卡片**上方**出现角色区块（顺序：Meta → 角色 → 转写 → Actions → 排练…）
    - `rg "RoleCardSection|role/read" src/app/session/[id]`有匹配
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
  <files>
    src/app/session/[id]/_ui/RoleCardSection.tsx
    src/app/session/[id]/page.tsx
  </files>
  <action>
    预设 `select` + 可选自定义短输入 +触发物 `input`；**保存** → `buildRoleCardText` + repo；展示预览摘要 + **进入朗读** `Link`（无卡时 disabled 或隐藏）。保存后 `onSaved` 刷新父级 `session` state。
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    ROLE-01 在详情页闭环。
  </done>
</task>

<task type="auto">
  <name>Task 2: /session/[id]/role/read 朗读页</name>
  <read_first>
    - src/app/session/[id]/review/[takeId]/page.tsx
    - src/app/_lib/sessionRepo.ts
  </read_first>
  <acceptance_criteria>
    - 存在 `src/app/session/[id]/role/read/page.tsx`（client）
    - 完成按钮调用 repo 标记时间戳并跳转 `/session/[id]/rehearsal`
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
  <files>
    src/app/session/[id]/role/read/page.tsx
  </files>
  <action>
    `getSessionById`；无 `roleCardText` 则内联提示 + 链回详情。正文：`roleCardText` 纯文本。`听一遍`：`speechSynthesis.speak`，结束前可 `cancel` 防泄漏（cleanup）。
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    ROLE-03 朗读模式可用。
  </done>
</task>

<task type="auto">
  <name>Task 3: 排练入口软阻塞 +可选排练页摘要</name>
  <read_first>
    - src/app/session/[id]/rehearsal/page.tsx
    - src/app/session/[id]/page.tsx
  </read_first>
  <acceptance_criteria>
    - 从会话详情进入排练时触发 D-06 confirm（有卡、未朗读）
    - 排练页可选展示一行当前角色气质/触发物（Claude discretion，若做则须读 session）
    - `npm run typecheck` 与 `npm run build` 退出码 0
  </acceptance_criteria>
  <files>
    src/app/session/[id]/page.tsx
    src/app/session/[id]/rehearsal/page.tsx
  </files>
  <action>
    将「进入排练」改为带 `onClick` 的按钮或 `RehearsalLink`：若 `session.roleCardText && !session.roleReadAloudCompletedAt` 则 `confirm(roleCopy.rehearsalSoftBlock)`，取消则 return；确认后 push。排练页 `useEffect` 拉 `getSessionById` 显示可选条。无卡：`confirm` 前可先 `alert` 引导保存。
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run build</automated>
  </verify>
  <done>
    D-06 与成功标准3 对齐。
  </done>
</task>

</tasks>

## Wave 2 Verification

```bash
npm run typecheck
npm run lint
npm run build
```

手工：保存角色 → 朗读页 → 完成 → 排练；有卡未朗读点排练 → confirm → 可取消 / 可继续。

---

*Plan 05-02 · Phase 05 wave 2*
