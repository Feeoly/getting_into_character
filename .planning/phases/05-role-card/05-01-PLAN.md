---
phase: 05-role-card
plan: 1
type: execute
wave: 1
depends_on:
  - 4
files_modified:
  - src/app/_lib/db.ts
  - src/app/_lib/sessionTypes.ts
  - src/app/_lib/sessionRepo.ts
  - src/app/session/[id]/_lib/buildRoleCardText.ts
  - src/app/session/[id]/_lib/roleCopy.ts
autonomous: true
requirements:
  - ROLE-02
user_setup: []
must_haves:
  truths:
    - Dexie v6；Session 含角色字段；Zod 向后兼容旧行
    - 纯函数由 mood+trigger 生成含三块标题的 roleCardText（本地、无网络）
    - sessionRepo 提供保存角色卡与标记朗读完成
  artifacts:
    - path: src/app/session/[id]/_lib/buildRoleCardText.ts
      provides: ROLE-02 模板正文
    - path: src/app/_lib/sessionRepo.ts
      provides: saveRoleCard / markRoleReadAloudComplete（命名以代码为准）
  key_links:
    - from: buildRoleCardText
      to: SESSION_SCHEMA.roleCardText
      via: sessionRepo.put
---

<objective>
落实 **05-CONTEXT D-03、D-04**：**Dexie v6** 扩展 **`sessions`** 行字段（角色状态、触发物、生成的 **`roleCardText`**、**`roleCardUpdatedAt`**、**`roleReadAloudCompletedAt`**）；**`SESSION_SCHEMA`** 全可选向后兼容。实现 **`buildRoleCardText`**（固定中文模板，含 **状态描述 / 可执行表达指令 / 禁忌** 三块）。**`sessionRepo`** 增加 **保存角色卡**与 **标记朗读完成**（`Date.now()`），供 wave 2 UI 调用。
</objective>

<context>
- **字段命名（实现可微调，须在 CONTEXT 语义内）：** 建议 `roleMoodPreset`（`z.enum` 或 `z.string`）、`roleMoodCustom`（可选 trim短文本）、`roleTrigger`（必填当保存时）、`roleCardText`、`roleCardUpdatedAt`、`roleReadAloudCompletedAt`（可选 number）。旧会话无字段 → `getSessionById` 仍解析成功。
- **预设气质：** 至少 4 项（与 CONTEXT 示例一致：自信、侃侃而谈、冷静、沉稳），文案在 **`roleCopy.ts`**。
- **模板：** 禁止 HTML；换行用 `\n`；指令为条列 `- ` 或 `1.`（择一统一）。
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户输入 → 卡片文本 | 仅字符串拼接，无 `dangerouslySetInnerHTML` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | I | 超长自定义 | mitigate | Zod max 长度 on moodCustom / trigger / cardText |
| T-05-01-02 | E | 迁移失败 | mitigate | version(6) 仅扩展 stores，不删表 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: SESSION_SCHEMA + db version(6)</name>
  <read_first>
    - .planning/phases/05-role-card/05-CONTEXT.md
    - src/app/_lib/sessionTypes.ts
    - src/app/_lib/db.ts
  </read_first>
  <acceptance_criteria>
    - `SESSION_SCHEMA` 含角色相关可选字段
    - `db.ts` 存在 `version(6)`，`sessions` 索引行与 v5 兼容（通常不变）
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
  <files>
    src/app/_lib/sessionTypes.ts
    src/app/_lib/db.ts
  </files>
  <action>
    为 `Session` 增加可选字段；`version(6)` 复制 v5 全表定义（Dexie 要求完整 stores声明）。
  </action>
  <verify>
    <automated>rg "version\\(6\\)" src/app/_lib/db.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    数据层可存角色卡与朗读时间戳。
  </done>
</task>

<task type="auto">
  <name>Task 2: roleCopy + buildRoleCardText</name>
  <read_first>
    - .planning/REQUIREMENTS.md — ROLE-02
  </read_first>
  <acceptance_criteria>
    - `buildRoleCardText` 导出且纯函数；输出含三个标题关键词（如「状态描述」「表达指令」「禁忌」——与 `roleCopy` 常量一致）
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
  <files>
    src/app/session/[id]/_lib/roleCopy.ts
    src/app/session/[id]/_lib/buildRoleCardText.ts
  </files>
  <action>
    `roleCopy`：section标题、预设标签 map、校验错误短句。`buildRoleCardText(input)`：合并 preset与 custom 为一句「状态」叙事；触发物融入指令与禁忌模板。
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    ROLE-02 文本结构可本地生成。
  </done>
</task>

<task type="auto">
  <name>Task 3: sessionRepo 持久化 API</name>
  <read_first>
    - src/app/_lib/sessionRepo.ts
    - src/app/_lib/sessionTypes.ts
  </read_first>
  <acceptance_criteria>
    - 至少两个导出函数：保存角色卡（更新 `roleCardText` 与 mood/trigger 字段）、标记朗读完成
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
  <files>
    src/app/_lib/sessionRepo.ts
  </files>
  <action>
    `get` → `put` 合并更新；不存在会话返回 `not_found`；校验用 Zod safeParse 防御脏数据。
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    Wave 2 可调用 repo 读写。
  </done>
</task>

</tasks>

## Wave 1 Verification

```bash
npm run typecheck
npm run lint
```

---

*Plan 05-01 · Phase 05 wave 1*
