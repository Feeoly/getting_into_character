---
phase: 06-ai
plan: 2
type: execute
wave: 2
depends_on:
  - 1
files_modified:
  - src/app/_lib/sessionRepo.ts
  - src/app/session/[id]/_ui/RoleCardReadOnly.tsx
  - src/app/session/[id]/role/read/page.tsx
  - src/app/session/[id]/_lib/roleCopy.ts
autonomous: true
requirements:
  - ROLE-01
  - ROLE-02
  - ROLE-03
user_setup: []
must_haves:
  truths:
    - 会话详情展示稿与朗读页均使用 getEffectiveRoleCardText
    - 未勾选同意前不可发起 /api/role/enhance；独立 localStorage 同意键 gic-ai-role-consent-v1
    - 采用新 AI 稿并覆盖展示时清除 roleReadAloudCompletedAt（强制重新朗读）
  artifacts:
    - path: src/app/session/[id]/_ui/RoleCardReadOnly.tsx
      provides: 同意门闸 + 增强按钮 + 本地/增强切换
    - path: src/app/_lib/sessionRepo.ts
      provides: saveRoleCardAi / patchSessionRoleCardPreference
  key_links:
    - from: RoleCardReadOnly
      to: POST /api/role/enhance
      via: fetch JSON
---

<objective>
落实 **06-RESEARCH §1、§5、§7**：在 **`RoleCardReadOnly`** 增加 **可选 AI 增强**：独立 **`gic-ai-role-consent-v1`** 勾选、`roleCopy`/`roleCopy` 风格 **`roleAi.*` 文案**（披露仅发送角色相关文字、不上传录音）。调用 **`/api/role/enhance`** 成功后 **`sessionRepo` 写入** `roleCardAiText`、`roleCardAiUpdatedAt`、`roleCardPreferAi: true`，并 **`roleReadAloudCompletedAt: undefined`**（清除朗读完成）。提供 **「使用本地原版 / 使用增强版」** 切换（更新 `roleCardPreferAi`）。**`/session/[id]/role/read`** 与 **TTS** 使用 **`getEffectiveRoleCardText(session)`** 而非裸读 `roleCardText`。无 `roleCardText` 时隐藏增强主操作。
</objective>

<context>
- **sessionRepo：** 新增 `saveRoleCardAiResult(sessionId, { roleCardAiText, roleCardAiUpdatedAt, roleCardPreferAi, clearReadAloud: true })` 或等价命名；内部 `getSessionById` → merge → `put`。
- **错误展示：** 503/502 时 `alert` 或行内 `text-red-600` 显示服务端 `error` 字段前 200 字；不打断本地稿。
- **加载态：** 请求中禁用按钮，防重复点击。
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 同意 → fetch | 与 ReviewChat 同模式，localStorage 门闸 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | I | XSS 模型输出 | mitigate | `<pre>` 或 `whitespace-pre-wrap` 文本展示，禁止 `dangerouslySetInnerHTML` |
| T-06-02-02 | E | 重复提交 | mitigate | loading 状态禁用按钮 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: sessionRepo 写入 AI 结果与偏好</name>
  <read_first>
    - src/app/_lib/sessionRepo.ts
    - src/app/_lib/sessionTypes.ts
  </read_first>
  <acceptance_criteria>
    - `rg "roleCardAiText" src/app/_lib/sessionRepo.ts` 退出码 0
    - `rg "roleCardPreferAi" src/app/_lib/sessionRepo.ts` 退出码 0
    - `rg "roleReadAloudCompletedAt" src/app/_lib/sessionRepo.ts` 退出码 0
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
  <files>
    src/app/_lib/sessionRepo.ts
  </files>
  <action>
    实现 `put` 合并；校验失败返回 `validation_error`。命名与现有 `markRoleReadAloudComplete` 风格一致。
  </action>
  <verify>
    <automated>rg "roleCardAiText|roleCardPreferAi" src/app/_lib/sessionRepo.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    UI 可持久化增强稿与偏好。
  </done>
</task>

<task type="auto">
  <name>Task 2: roleCopy 文案 + RoleCardReadOnly UI</name>
  <read_first>
    - src/app/session/[id]/_ui/RoleCardReadOnly.tsx
    - src/app/session/[id]/review/[takeId]/_ui/ReviewChat.tsx
    - src/app/session/[id]/_lib/roleCopy.ts
  </read_first>
  <acceptance_criteria>
    - `roleCopy.ts`（或并列 `roleAi` 导出）含：`roleAi.disclosure`、`roleAi.consentLabel`、`roleAi.enhanceButton`、`roleAi.useLocal`、`roleAi.useAi`、`roleAi.consentRequired`（字符串常量存在即可）
    - `RoleCardReadOnly.tsx`：`grep -q "gic-ai-role-consent-v1" src/app/session/[id]/_ui/RoleCardReadOnly.tsx` 退出码 0
    - 主 `<pre>` 展示 `getEffectiveRoleCardText(session) ?? "—"`
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
  <files>
    src/app/session/[id]/_lib/roleCopy.ts
    src/app/session/[id]/_ui/RoleCardReadOnly.tsx
  </files>
  <action>
    勾选同意读写在 `useEffect` 初始化从 `localStorage.getItem('gic-ai-role-consent-v1') === '1'`。点击「AI 增强」：`fetch('/api/role/enhance',{method:'POST',body:JSON.stringify({draftText: session.roleCardText ?? '', trigger: session.roleTrigger, moodCustom: session.roleMoodCustom, moodPreset: session.roleMoodPreset})})`，解析 `{ content }`，调用 `sessionRepo` 保存。切换按钮仅在有 `roleCardAiText` 时显示。
  </action>
  <verify>
    <automated>rg "getEffectiveRoleCardText" src/app/session/[id]/_ui/RoleCardReadOnly.tsx</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    详情页完成同意门闸与增强流程。
  </done>
</task>

<task type="auto">
  <name>Task 3: role/read 使用 effective 文本</name>
  <read_first>
    - src/app/session/[id]/role/read/page.tsx
    - src/app/_lib/sessionTypes.ts
  </read_first>
  <acceptance_criteria>
    - `rg "getEffectiveRoleCardText" --glob "role/read/page.tsx" src/app/session` 退出码 0
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
  <files>
    src/app/session/[id]/role/read/page.tsx
  </files>
  <action>
    将 `session.roleCardText` 替换为 `const card = getEffectiveRoleCardText(session)`；`onListen` 与页面展示均用 `card`。
  </action>
  <verify>
    <automated>rg "getEffectiveRoleCardText" --glob "role/read/page.tsx" src/app/session</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    ROLE-03 朗读与详情展示稿一致。
  </done>
</task>

</tasks>

## Wave 2 Verification

```bash
npm run typecheck
npm run lint
```

手工 UAT：配置 `BAILIAN_API_KEY` → 会话详情勾选同意 → 增强成功 → 朗读页文本与详情一致 → 切换本地版后朗读文本变回本地稿。

---

*Plan 06-02 · Phase 06 wave 2*
